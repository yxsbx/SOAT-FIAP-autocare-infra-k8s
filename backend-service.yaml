apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: autocarehub
spec:
  type: ClusterIP
  selector:
    app: autocarehub-api
  ports:
    - name: http
      port: 8080
      targetPort: http
