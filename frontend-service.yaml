apiVersion: v1
kind: Service
metadata:
  name: autocarehub-web
  namespace: autocarehub
spec:
  type: ClusterIP
  selector:
    app: autocarehub-web
  ports:
    - name: http
      port: 8080
      targetPort: http
